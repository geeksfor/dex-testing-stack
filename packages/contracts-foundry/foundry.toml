[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
ffi = false
fs_permissions = [{ access = "read", path = "./" }]

[rpc_endpoints]
mainnet = "${ETH_RPC_URL}"

[invariant]
runs = 64
depth = 32
fail_on_revert = true